# Simple Snake Game

A tiny browser-based snake game written with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` directly in your browser, or serve this folder with a simple static server:

```bash
python3 -m http.server 8000
```

Then visit <http://localhost:8000>.

## Controls

- Arrow keys: move snake
- `R`: restart after game over
